# ── Frontend .env.local ───────────────────────────────────────────────────────
# Copy to .env.local — Vercel reads this automatically via the dashboard.

# URL of your deployed Node.js backend (no trailing slash)
NEXT_PUBLIC_API_URL=http://localhost:4000
